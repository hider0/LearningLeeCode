# 学习指南：配套方法论教程

> 本文档是「LeetCode 刷题训练器」的配套教程，按章节讲解通用解题方法论。
> 题库中所有题面均为**学习目的的改写**，未复制任何官方题面文字；算法思想本身属于公开知识。
> 建议配合训练器使用：读一章 → 刷一章 → 错题进错题本 → 不明之处用「追问」。

## 怎么用这个训练器学习

推荐的闭环：**读方法论 → 独立写码 → 提交判题 → 对照讲解 → 错了进错题本 → 追问疑点 → 隔天重做错题**。

几个容易被忽略的技巧：

- 代码里写 `print(...)` 调试，输出会显示在判题结果里，比在本地 IDE 还方便；
- 判题只看输出对错，不看写法优劣——先写暴力解确保正确，再优化，是很好的习惯；
- 错题本里的题**隔天再做一次**，比当场看懂答案有效得多；
- 每章的「提示」（hints）是循序渐进的，卡壳时先看提示，别直接看讲解。

## 章节地图

| 章节 | 核心模式 | 关键词 |
|---|---|---|
| 1. 数组与哈希 | 用空间换时间 | 哈希表查重、补数查找、分组 key |
| 2. 字符串 | 双指针与滑动窗口 | 对撞指针、窗口扩张与收缩 |
| 3. 链表 | 指针操作 | 哑节点、前后指针、倒数第 N 个 |
| 4. 栈 | 后进先出的匹配 | 括号配对、表达式求值、路径规整 |
| 5. 二叉树 | 递归与层序遍历 | 递归三要素、DFS/BFS |
| 6. 动态规划 | 状态转移 | 状态定义、转移方程、滚动变量 |

## 第 1 章 · 数组与哈希

**一句话总结：凡是「查找」「去重」「计数」「分组」，先想哈希表。**

哈希表的本质是用 O(n) 的空间把 O(n) 的查找降到 O(1)。三类典型用法：

- **存在性查询**：两数之和的「查 target - x 是否见过」——注意先查表再插入，避免元素自己匹配自己；
- **计数与去重**：`set` 判重、`dict` 计数，存在重复元素一行搞定；
- **设计分组的 key**：字母异位词分组的精髓是找到「同类事物共有的规范化形式」（排序后的字符串）。

**常见错误**：边遍历边修改正在遍历的结构；忘记处理重复元素导致的 key 覆盖。

## 第 2 章 · 字符串

**一句话总结：双指针处理对称性，滑动窗口处理「连续子串 + 约束」。**

- **对撞双指针**：判断回文、反转这类有左右对称结构的问题，两个指针从两端向中间走，O(n) 一趟解决。先想「需要跳过哪些字符」（预处理或边走边过滤）。
- **滑动窗口**：凡是「最长/最短连续子串满足某条件」，模板是：
  1. 右指针扩张窗口，把新元素记入状态；
  2. 当约束被破坏，左指针收缩窗口直到约束恢复；
  3. 每一步更新答案。
  关键细节：左指针只能右移不能回退（无重复字符最长子串里 `left = max(left, ...)` 就是防回退）。

**常见错误**：窗口收缩条件写成 `if` 而不是 `while`；忘记大小写/字符过滤的边界。

## 第 3 章 · 链表

**一句话总结：链表题的三件法宝——哑节点、双指针、画图。**

- **哑节点（dummy）**：当头节点可能被删除或变更时，在头部前面挂一个假节点，统一处理逻辑，最后返回 `dummy.next`。合并链表、删除节点都用它；
- **前后双指针**：反转链表就是 `prev / cur / next` 三个指针的循环换向，务必先在纸上画清楚再写；
- **快慢指针**：找倒数第 N 个节点 = 快指针先走 N 步，再同步走，快指针到头时慢指针就是目标。

**常见错误**：指针换向顺序错导致断链；忘记处理空表和单节点；返回了错误的节点（该返回新头却返回了旧头）。

## 第 4 章 · 栈

**一句话总结：当「后出现的元素先被处理」时，用栈。**

识别信号：配对/嵌套结构（括号）、逆序处理（表达式、路径）、需要记住「上一个未处理的元素」。

- **括号匹配**：左括号入栈，右括号弹栈比对；最后栈必须为空；
- **逆波兰表达式**：数字入栈，运算符弹出两个数计算后压回。注意 **Python 的 `//` 是向下取整，题目要求向零截断**，要用 `int(a / b)`；
- **路径简化**：按 `/` 切分，目录名入栈，`..` 弹栈，`.` 和空串跳过；`..` 在栈空时不报错。

**常见错误**：弹空前栈没判空；操作数弹出顺序写反（先弹出的是右操作数）。

## 第 5 章 · 二叉树

**一句话总结：二叉树 = 递归的定义，先写递归三要素。**

递归三要素：**终止条件**（空节点返回什么）、**本层做什么**、**子问题返回什么**。
写递归时不要试图在大脑里展开全部调用栈，只保证「当前层逻辑正确 + 信任子调用」。

- **深度/路径类**（最大深度）：`1 + max(左, 右)`，一行递归；
- **结构变换类**（翻转树）：先递归处理子树还是先交换当前节点，很多情况下都可以，但要想清楚顺序对结果的影响；
- **层序遍历**：BFS 用队列，每轮记录队列长度（这一层的节点数），逐层展开；
- 本训练器中树用**层序数组**表示（`None` 为空节点），读题时先把数组在纸上画成树。

**常见错误**：忘记空树返回；BFS 没在每轮开头固定队列长度导致层级混乱。

## 第 6 章 · 动态规划

**一句话总结：DP 四步——定义状态、写转移方程、定初始值、确定遍历顺序。**

- **爬楼梯**：`f(n) = f(n-1) + f(n-2)`，斐波那契的马甲；会这一步就会用「滚动变量」把空间压到 O(1)；
- **打家劫舍**：不能偷相邻两家 → `f(i) = max(f(i-1), f(i-2) + nums[i])`，即「偷或不偷第 i 家」的二选一。它演示了 DP 最核心的思想：**最优子结构 + 无后效性**；
- **零钱兑换**：完全背包式 DP，`f(amount) = min(f(amount - coin)) + 1`，注意「凑不出」要用一个不可能的值（如 `amount + 1`）表示，最后特判。

**判断能不能用 DP**：问题能拆成「规模更小的同类问题」，且答案只依赖子问题答案（无后效性），就值得试。

**常见错误**：初始值设错（`f(0)` 往往是突破口）；把「凑不出」的情况和 0 混淆。

## 刷题的通用心法

1. **先暴力，再优化**：暴力解能帮你想清楚问题的结构，也是面试时的兜底方案；
2. **复杂度意识**：每写完一题，说出它的时间/空间复杂度，并问自己「还能不能再降」；
3. **错题是资产**：错题本里的每一道题，标注「当时卡在哪」（思路错 or 实现错），比刷十道新题更有价值；
4. **讲出来**：能用追问功能把疑点问清楚、能用自己的话复述思路，才算真的会了。

{
  "chapter": "第 2 章 · 字符串",
  "problems": [
    {
      "id": "reverse-string",
      "title": "344. 反转字符串",
      "difficulty": "简单",
      "description": "编写一个函数，其作用是将输入的字符列表反转。\n\n原题要求在**原地**修改输入列表、使用 O(1) 的额外空间；为便于判题，本题要求**返回反转后的新列表，而非原地修改**。\n\n**提示**：可以用 `s[::-1]` 一行解决，但建议先用双指针手动实现，体会交换的过程。",
      "examples": [
        {"input": "s = [\"h\",\"e\",\"l\",\"l\",\"o\"]", "output": "[\"o\",\"l\",\"l\",\"e\",\"h\"]"},
        {"input": "s = [\"H\",\"a\",\"n\",\"n\",\"a\",\"h\"]", "output": "[\"h\",\"a\",\"n\",\"n\",\"a\",\"H\"]"},
        {"input": "s = [\"a\"]", "output": "[\"a\"]"}
      ],
      "entry": "reverseString",
      "starter": "class Solution:\n    def reverseString(self, s: list[str]) -> list[str]:\n        # 在这里编写你的代码\n        pass\n",
      "tests": [
        {"args": [["h", "e", "l", "l", "o"]], "expected": ["o", "l", "l", "e", "h"]},
        {"args": [["H", "a", "n", "n", "a", "h"]], "expected": ["h", "a", "n", "n", "a", "H"]},
        {"args": [["a"]], "expected": ["a"]},
        {"args": [[]], "expected": []}
      ],
      "compare": "exact",
      "hints": [
        "最直接的写法是切片 `s[::-1]`，但这相当于「调库」，面试中通常不允许。",
        "用两个指针 left、right 分别指向列表首尾，交换两个元素后向中间靠拢。",
        "当 left >= right 时交换结束，此时整个列表已被反转。"
      ],
      "explanation": "**核心思路：双指针交换**\n\n要反转一个列表，只需让首尾两两交换：第 0 个与倒数第 1 个交换，第 1 个与倒数第 2 个交换……直到两个指针在中间相遇。\n\n**算法步骤**\n1. 初始化 `left = 0`，`right = len(s) - 1`。\n2. 当 `left < right` 时：交换 `s[left]` 和 `s[right]`，然后 `left += 1`、`right -= 1`。\n3. 循环结束后列表即反转完成，返回列表。\n\n原题要求原地修改；为便于判题，这里把交换后的列表作为返回值返回（交换同样发生在原列表上，返回新列表或交换后的列表均可通过判题）。\n\n**复杂度**：时间 O(n)，每个元素最多被交换一次；空间 O(1)，只用了两个指针。\n\n**参考代码**\n```python\nclass Solution:\n    def reverseString(self, s: list[str]) -> list[str]:\n        left, right = 0, len(s) - 1\n        while left < right:\n            s[left], s[right] = s[right], s[left]\n            left += 1\n            right -= 1\n        return s\n```\n\n一行写法（了解即可）：`return s[::-1]`。"
    },
    {
      "id": "valid-palindrome",
      "title": "125. 验证回文串",
      "difficulty": "简单",
      "description": "如果在将所有大写字母转换为小写字母、并移除所有**非字母数字字符**之后，一个短语正着读和反着读都一样，则它就是一个**回文串**。\n\n字母和数字都属于字母数字字符。注意：下划线 `_` 等符号属于非字母数字字符，需要剔除。\n\n给你一个字符串 `s`，如果它是回文串，返回 `true`；否则，返回 `false`。",
      "examples": [
        {"input": "s = \"A man, a plan, a canal: Panama\"", "output": "true", "explanation": "剔除并转小写后得到 \"amanaplanacanalpanama\"，它是回文串。"},
        {"input": "s = \"race a car\"", "output": "false", "explanation": "剔除后得到 \"raceacar\"，不是回文串。"},
        {"input": "s = \" \"", "output": "true", "explanation": "剔除非字母数字字符后为空字符串，空串也是回文串。"}
      ],
      "entry": "isPalindrome",
      "starter": "class Solution:\n    def isPalindrome(self, s: str) -> bool:\n        # 在这里编写你的代码\n        pass\n",
      "tests": [
        {"args": ["A man, a plan, a canal: Panama"], "expected": true},
        {"args": ["race a car"], "expected": false},
        {"args": [" "], "expected": true},
        {"args": ["0P"], "expected": false},
        {"args": ["ab_a"], "expected": true}
      ],
      "compare": "exact",
      "hints": [
        "先用 `str.isalnum()` 过滤掉非字母数字字符，再统一转成小写。",
        "过滤后的字符串如果是回文串，必然等于它自身的反转：`t == t[::-1]`。",
        "也可以用双指针：left 从前往后、right 从后往前，各自跳过非法字符，遇到不相等的字符就返回 false。"
      ],
      "explanation": "**核心思路：过滤 + 判回文**\n\n题目等价于两步：\n1. **预处理**：只保留字母和数字（`str.isalnum()` 判断），并统一转成小写（`str.lower()`）。\n2. **判回文**：处理后的字符串 `t` 是回文串，当且仅当 `t == t[::-1]`。\n\n**算法步骤**\n1. 遍历 `s`，把满足 `c.isalnum()` 的字符小写后收集起来，得到 `t`。\n2. 返回 `t == t[::-1]`。\n\n**正确性要点**：数字也要保留（例如 `\"0P\"` 过滤后为 `\"0p\"`，反转不同，应返回 false）；下划线等非字母数字字符必须剔除（例如 `\"ab_a\"` 过滤后为 `\"aba\"`，是回文串）。\n\n**复杂度**：时间 O(n)，反转与比较都是线性；空间 O(n)，存过滤后的字符串。\n\n**参考代码**\n```python\nclass Solution:\n    def isPalindrome(self, s: str) -> bool:\n        t = \"\".join(c.lower() for c in s if c.isalnum())\n        return t == t[::-1]\n```\n\n**进阶（O(1) 空间）**：用双指针直接在原串上比对，`left`/`right` 各自跳过 `isalnum()` 为假的字符，逐对比较小写后的字符，一旦不等就返回 false；指针相遇则返回 true。"
    },
    {
      "id": "longest-substring",
      "title": "3. 无重复字符的最长子串",
      "difficulty": "中等",
      "description": "给定一个字符串 `s`，请你找出其中不含有重复字符的**最长子串**的长度。\n\n注意：子串是连续的，与子序列不同。例如 `\"pwwkew\"` 中 `\"pwke\"` 是子序列但不是子串，答案是连续子串 `\"wke\"` 的长度 3。",
      "examples": [
        {"input": "s = \"abcabcbb\"", "output": "3", "explanation": "无重复字符的最长子串是 \"abc\"，长度为 3。"},
        {"input": "s = \"bbbbb\"", "output": "1", "explanation": "最长子串是 \"b\"，长度为 1。"},
        {"input": "s = \"pwwkew\"", "output": "3", "explanation": "最长子串是 \"wke\"，长度为 3。注意 \"pwke\" 不是子串。"},
        {"input": "s = \"\"", "output": "0"}
      ],
      "entry": "lengthOfLongestSubstring",
      "starter": "class Solution:\n    def lengthOfLongestSubstring(self, s: str) -> int:\n        # 在这里编写你的代码\n        pass\n",
      "tests": [
        {"args": ["abcabcbb"], "expected": 3},
        {"args": ["bbbbb"], "expected": 1},
        {"args": ["pwwkew"], "expected": 3},
        {"args": [""], "expected": 0},
        {"args": ["au"], "expected": 2},
        {"args": ["dvdf"], "expected": 3},
        {"args": ["tmmzuxt"], "expected": 5}
      ],
      "compare": "exact",
      "hints": [
        "枚举所有子串再判重是 O(n²) 起步，太慢。试着维护一个「不含重复字符的窗口」。",
        "用左右指针 left、right 表示窗口，右指针每前进一步，就把新字符加入窗口；如果造成重复，就收缩左边界。",
        "收缩的关键：用哈希表记录每个字符**最近一次出现的下标**，重复时 left 直接跳到「上次出现位置 + 1」和当前 left 的较大值，不必一步步挪。",
        "每次移动右指针后，用 right - left + 1 更新答案。"
      ],
      "explanation": "**核心思路：滑动窗口 + 哈希表记录最近出现位置**\n\n维护一个窗口 `[left, right]`，保证窗口内没有重复字符。右指针 `right` 从左到右扫描字符串：\n\n- 如果字符 `s[right]` 之前在窗口内出现过（即 `last[c] >= left`，`last[c]` 是该字符上一次出现的下标），说明有重复，把左边界直接跳到 `last[c] + 1`，一步收缩到位；\n- 否则无需收缩。\n- 更新 `last[c] = right`，并用当前窗口长度 `right - left + 1` 刷新最大值。\n\n**为什么 left 要取 `max(left, last[c] + 1)`**：`last[c]` 可能是窗口之外的旧记录（左边界已经越过它），这时不能往回跳。例如 `\"tmmzuxt\"`：扫描到第二个 `m` 时 left 跳到 2；之后窗口 `[m, z, u, x, t]` 长度为 5，即答案。\n\n**复杂度**：时间 O(n)，每个字符只被左右指针各访问一次；空间 O(k)，k 为字符集大小（本题至多为所有可见字符数）。\n\n**参考代码**\n```python\nclass Solution:\n    def lengthOfLongestSubstring(self, s: str) -> int:\n        last = {}  # 字符 -> 最近一次出现的下标\n        left = 0\n        ans = 0\n        for right, c in enumerate(s):\n            if c in last and last[c] >= left:\n                left = last[c] + 1\n            last[c] = right\n            ans = max(ans, right - left + 1)\n        return ans\n```"
    }
  ]
}
